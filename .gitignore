# Allow-list .gitignore: ignore everything, then un-ignore what we want tracked.

# Ignore every top-level entry by default.
/*

# Re-include the entries we want to track.
!/.gitignore
!/README.md
!/LICENSE.md
!/package.json
!/tsconfig.json
!/bun.lockb
!/src/
